# TBD: Active Backlog Exhausted

## Problem
All High Priority and Medium Priority backlog items are complete. The Icebox is explicitly human-controlled, so the agent loop cannot promote or start those items without Product Owner direction.

## Current State
- The ASP.NET Core Minimal API, static frontend, live API swap, and frontend response parsing work are complete.
- The active queues in `docs/state/backlog.md` contain no remaining unchecked items.

## Icebox Proposals
1. Promote `Explore adding conversation memory so follow up questions can be asked about an uploaded image.`
   - This is the most natural product extension after structured response rendering because it deepens the image-analysis workflow without changing deployment shape.
2. Promote `Add Dockerfile support for automated container deployments.`
   - This is the most natural engineering extension if the demo needs recruiter-friendly deployment or repeatable hosting.

## Needed Human Decision
Move one or more Icebox items into the High or Medium Priority queue, or add a new active backlog item.
